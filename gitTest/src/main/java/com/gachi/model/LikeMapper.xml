<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gachi.model.MemberMapper">

	<!-- 동일 게시글 좋아요 여부 검색 -->
	<select id="likeCheck" parameterType="com.gachi.controller.LikeUpdate" resultType="int">
		select count(*) from tbl_likes where user_id=#{user_id} and post_id=#{post_id}
	</select>
	
	<!-- 게시글 좋아요 -->
	<insert id="likeUpdate" parameterType="com.gachi.controller.LikeUpdate">
		insert into tbl_likes(post_id, user_id, created_at) values(#{post_id}, #{user_id}, now())
	</insert>

	<!-- 게시글 좋아요 취소 -->
	<delete id="likeDelete" parameterType="com.gachi.controller.LikeUpdate">
		delete from tbl_likes where user_id=#{user_id} and post_id=#{post_id}
	</delete>

</mapper>